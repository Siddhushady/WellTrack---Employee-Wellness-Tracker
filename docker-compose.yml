version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: welltrack_db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: "Calcite*1234"
      POSTGRES_DB: emotion_detection
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  pgdata:
